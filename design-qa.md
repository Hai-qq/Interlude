# 当前设计验证

final result: passed

用户确认的纸面、手绘数字与胖雀方案保存在 [选定参考](design/editorial-reference/selected.png)。当前原生窗口截图为 [current.png](design/editorial-reference/current.png)：320×320 逻辑像素、640×640 截图，剩余 18 分钟 / 已用 27 分钟。原参考为 1254×1254；设计整体缩小是后续用户确认的调整。

数字由独立绘制的 0–9 和冒号组合，中文为霞鹜文楷；纸面、石墨与橙色配色保留。日常、休息和归来使用相同鸟尺寸、定位和 DOM；入场只淡入，避免缩放跳变。休息数字与鸟互不遮挡，设置控制完整可见。

本次整理删除旧版本、未采用设计、未使用组件和历史脚本，不改变当前视觉。整理前后的日常及设置原生截图逐字节比较一致。类型检查、56 项单元测试及生产构建通过。完整原生流程结果在本机 `reports/current/panel-qa.json`，不将机器路径与试运行数据提交到 Git。

原生检查覆盖窗口尺寸、位置、200% 设置缩放、草稿保护、手绘数字加载、暂停恢复、同一鸟 DOM 和边界尺寸、休息与归来、推迟及后台重建。运行 `npm run build && npm run test:e2e` 可重现。

保留的已知差异：图生字形不是原参考逐像素复制，中文也不是从图中提取的完整字体；图集中的自然笔触差异保留。当前没有未解决的 P0/P1/P2 视觉问题。
